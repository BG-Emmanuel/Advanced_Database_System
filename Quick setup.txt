# 1. Extraire le ZIP
unzip buy237.zip
cd buy237_full

# 2. Backend
cd backend
npm install
cp .env.example .env
# → Éditer .env : mettre DATABASE_URL (Supabase) et JWT_SECRET
npm run dev          # Lance sur http://localhost:5000

# 3. Frontend (nouveau terminal)
cd frontend
npm install
npm start            # Lance sur http://localhost:3000